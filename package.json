{
  "name": "pianki",
  "version": "0.1.18",
  "description": "Anki卡片快速制作工具",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "bump": "node scripts/bump-version.mjs",
    "retag": "node scripts/retag.mjs",
    "release": "node scripts/release.mjs",
    "start": "npm run start --workspace=backend",
    "package:backend": "npm run build --workspace=backend && npm run prepare:native --workspace=backend && pkg backend/dist/index.js -t node20-macos-arm64,node20-macos-x64,node20-linux-x64,node20-linux-arm64,node20-win-x64 -o src-tauri/binaries/pianki-backend --compress Brotli --public-packages \"anki-apkg-export,marked,better-sqlite3,@mongodb-js/zstd\" --no-bytecode --public --config backend/package.json",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "keywords": [
    "anki",
    "flashcards",
    "learning"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/prehisle/pianki.git"
  },
  "author": "prehisle",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.2",
    "@yao-pkg/pkg": "^6.10.1",
    "concurrently": "^8.2.2",
    "rollup": "4.21.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1"
  }
}
